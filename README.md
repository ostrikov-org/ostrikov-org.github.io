# Hello, world!
This is the code for `ostrikov.org` website.

# Serving static files locally
## Using serve

You can serve static files using a simple tool called [serve](https://www.npmjs.com/package/serve)

```bash
npx serve src/
```

Then connect to a printed address and you're good to go!
